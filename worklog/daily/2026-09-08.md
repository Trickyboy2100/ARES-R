# 2026-09-08 开发日志

- 为 Terminal 事件、cuRobo 全阶段、原生 ServoJ 反馈和 Dashboard 增加统一墙钟/单调时钟记录。
- 将 cuRobo 规划参数集中到 `curobo.planning`，加入范围检查并把完整参数固化到每次请求和结果。
- 审计锁定版本的 `MotionPlannerCfg.create`、`plan_cspace`、`update_world` 与默认优化器 YAML，形成规划耗时参数清单和优化顺序。
- 在 .32 GPU 上用固定历史请求完成五次纯规划基准，未连接或控制机械臂；CUDA Graph 显示约 20% 求解耗时下降，等待多场景回归后再决定默认值。
- 修复 ART 同步 MoveJ 卡死：改为非阻塞下发、100 ms 实际关节反馈监督、动态超时及异常自动 `motion_abort`，并修正 3°边界浮点误判。
- 完成 `ready` 双臂姿态实机标定：右臂采用 cuRobo `plan_cspace` 后由原生 SDK ServoJ 执行，左臂采用九段受监督 MoveJ；最终 BODY TCP 分别为 `(0.380,+0.160,1.200)m` 与 `(0.380,-0.160,1.200)m`。
- 右臂首次 3× ServoJ 在 21.5% 因跟随误差超过 0.2°自动中止并完成清理；从实时位置以 2×重新规划后执行完成，未放宽安全门槛。
- `ready` 状态更新为 `commissioned`，ART 增加 `pose go ready left direct` 与 `pose go ready right curobo` 两条已验证执行路线。
- 完成 `zero / forward / up / side` 双臂实机 commissioning；废弃无解的固定末端姿态目标，改用符合侧装 Mini2 关节语义的简洁姿态定义，并验证相邻路径 TCP 不进入 14 cm 身体中线禁区。
- `side` 双臂到位并退出 SDK 后，后续复读发现两臂被外部来源改姿，TCP 均处于中线禁区；检查时无 ARES-R/JAKA 控制进程或活动控制器 TCP 连接，未抢占、未自动恢复，保留现场排查。
- 现场确认手动摆动不影响 `side` commissioning 结论，`side` 目测姿态合格；本次收尾不再下发机械臂动作。
- 手动姿态到右臂 `ready` 的实测最大关节差为 J4 136.3°；将 cuRobo reposition 的软件/原生双层上限由 120°审慎提高到 150°，保留软限位、碰撞、中线、速度、加速度、TCP 行程及跟随误差门控。
- ART 命名姿态指令加入显式速度：cuRobo 接受 `2x|3x`（默认稳定 2x），直接 MoveJ 接受 `1deg/s`～`5deg/s`，速度单位必须写入指令以避免歧义。
- 接入 EpicEye SDK 单帧点云采集与二进制 PLY 审计，显式固化毫米到米的换算、有效率、范围、哈希和 planning-ready 阻断原因。
- 落地方案二数据边界：严格校验 ATOM 的 BODY-frame AABB JSON，并保守转换为左右臂 URDF base_link cuboid 场景；相机外参、自体滤除与 ROI 未 commissioning 前禁止原始点云进入规划。
