# 2026-09-08 开发日志

- 为 Terminal 事件、cuRobo 全阶段、原生 ServoJ 反馈和 Dashboard 增加统一墙钟/单调时钟记录。
- 将 cuRobo 规划参数集中到 `curobo.planning`，加入范围检查并把完整参数固化到每次请求和结果。
- 审计锁定版本的 `MotionPlannerCfg.create`、`plan_cspace`、`update_world` 与默认优化器 YAML，形成规划耗时参数清单和优化顺序。
- 在 .32 GPU 上用固定历史请求完成五次纯规划基准，未连接或控制机械臂；CUDA Graph 显示约 20% 求解耗时下降，等待多场景回归后再决定默认值。
